/**
 * @param {string} s
 * @param {string} t
 * @return {string}
 */
var minWindow = function(s, t) {
    // const duplicateCheck = {};
    // let range = [],

    // for (let i = 0; i < s.length; i++) {
    //     for (let e = i; e < s.length; e++)
    // }
};